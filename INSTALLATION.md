# Quick Start Guide - Prompt Drawer Installation

## Step 1: Download the Extension
You have two options:
- Use the `prompt-drawer.zip` file (extract it first)
- Use the `prompt-drawer-extension` folder directly

## Step 2: Install in Chrome

1. Open Chrome and go to: **chrome://extensions/**
2. Turn ON **Developer mode** (toggle switch in the top-right corner)
3. Click the **Load unpacked** button
4. Navigate to and select the `prompt-drawer` folder (the one containing manifest.json)
5. Click **Select Folder**

## Step 3: Pin the Extension (Recommended)

1. Look for the puzzle piece icon 🧩 in Chrome's toolbar (top-right)
2. Find "Prompt Drawer" in the list
3. Click the pin icon 📌 next to it
4. The Prompt Drawer icon will now appear in your toolbar!

## Step 4: Try It Out

1. Click the Prompt Drawer icon in your toolbar
2. The floating drawer should appear!
3. Click any snippet to insert it
4. Try filtering by category
5. Right-click any text on a webpage and choose "Save to Prompt Drawer"

## Having Issues?

**Extension won't load?**
- Make sure you selected the correct folder (it should contain manifest.json)
- Check that Developer mode is enabled
- Look for error messages in the extensions page

**Drawer won't appear?**
- Refresh the webpage you're on
- Make sure the extension is enabled (toggle switch is ON)
- Check if there are any console errors (F12 > Console tab)

**Need help?**
Check the full README.md for detailed documentation and troubleshooting tips!

---

Enjoy using Prompt Drawer! 🎉
